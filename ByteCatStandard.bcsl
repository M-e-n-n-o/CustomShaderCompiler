#shader "ByteCatStandard.bcsl"

uniform mat4 _modelMatrix;
uniform mat4 _viewMatrix;
uniform mat4 _projectionMatrix;

uniform float _time;

vertex
{
    mat4 objectToClipSpace(vec4 pos)
    {
        return _projectionMatrix * _viewMatrix * _modelMatrix * pos;
    }
}